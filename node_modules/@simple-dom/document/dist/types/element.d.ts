import { SimpleAttr, SimpleElement, SimpleNodeType } from '@simple-dom/interface';
import Node from './node';
export default class Element extends Node implements SimpleElement {
    nodeType: SimpleNodeType.ELEMENT_NODE;
    attributes: SimpleAttr[];
    constructor(tagName: string);
    readonly tagName: string;
    getAttribute(name: string): string | null;
    setAttribute(name: string, value: any | undefined | null): void;
    removeAttribute(name: string): void;
    protected _cloneNode(): Element;
}
